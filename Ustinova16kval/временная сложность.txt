    try:
        with open("входной.txt", "r", encoding="utf-8") as file:
            lines = file.readlines() <--
    except FileNotFoundError:
        print("Файл 'входной.txt' не найден.")
        return

Метод читает весь файл построчно и возвращает список строк. Он проходит по файлу один раз, обрабатывая каждую строку ровно один раз.
Временная сложность О(n). В данном контексте n — это количество строк во входном файле